#!/bin/sh

add_arg "$(k_arg BASE_SHA)"
add_arg "$(v_arg BASE_SHA)"
add_arg "$(k_arg PR)"
add_arg "$(v_arg PR)"
add_arg "$(k_arg SLUG)"
add_arg "$(v_arg SLUG)"
add_arg "$(k_arg SERVICE)"
add_arg "$(v_arg SERVICE)"
